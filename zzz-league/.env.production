# Committed on purpose -- this is a public URL, not a secret (every browser sees it in the
# network tab regardless of where it's configured). Vite loads this automatically for any
# `npm run build`, local or CI, no GitHub Actions variable needed.
VITE_API_BASE_URL=https://zenless-league-api-696399405313.europe-west1.run.app
